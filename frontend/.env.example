# Only needed when the SPA and the API are deployed to different origins.
# On Vercel both are served from one project, so the default relative path
# (/api/v1) is correct and this file is not required.
# VITE_API_BASE_URL=https://your-api.example.com/api/v1
